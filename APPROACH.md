# פרויקט דה-דופליקציה של מוצרים ואיחוד מחירים - סיכום הגישה

מסמך קצר שמסביר את העיצוב ואת ההיגיון מאחוריו. לקריאה מעמיקה יותר עם
היסטוריית איטרציות, דוגמאות מלאות, גרפים ומטריקות מלאות, ראה את
[APPROACH-extended.md](APPROACH-extended.md).

> **פסק הדין בקצרה:**
> - בחרתי ב-`pipeline` היברידי של `embeddings` ואז `LLM`, לא באחת משתי הגישות לבד.
> - השתמשתי בדאטהסט סינתטי מבוקר של 131 רישומים כדי לאפשר גם מדידה וגם בדיקה ידנית של מקרי קצה.
> - גרסת ברירת המחדל היא v2, והיא חוסכת כ-22% מקריאות ה-`API` מול v1 וגם משיגה `F1` מעט טוב יותר.

---

## 1. הבעיה

> מקבלים רשימת מוצרים עם כפילויות ושמות לא עקביים (לדוגמה, `Samsung S23` ו-`סמסונג גלקסי S23`).
> צריך לכתוב סקריפט שמאחד את הכפילויות ומבטיח שהמחיר הזול ביותר מוצג ללקוח.

שלושה דברים מסתתרים בפסקה הזאת: **התאמה בין שפות** (שמות בעברית, אנגלית
ותעתיק עבור אותו מוצר), **אי-עקביות בשמות** (קיצורים שונים, השמטות מותג,
`256GB` מול `256 ג'יגה`), ו**איחוד מחירים** (החלק הקל, ברגע שיודעים אילו
רישומים הם כפילויות).

## 2. למה גישה היברידית

שלוש דרכים כנות לתקוף את הבעיה:

- **רק `LLM`** - מטפל בעברית באופן טבעי, אבל יקר, איטי ולא דטרמיניסטי
  בקנה מידה גדול. לא ישים למיליוני מוצרים.
- **רק `embeddings`** - זול וסקיילבילי, אבל התאמת דמיון טהורה תאחד את
  `Galaxy S25` עם `Galaxy S25 FE` כי המחרוזות נראות כמעט זהות.
- **היברידית (נבחרה)** - ה-`embeddings` עושים את הסינון הזול והסקיילבילי,
  וה-`LLM` רואה רק את המקרים המעורפלים ששלב ה-`embeddings` לא הצליח להכריע.
  הטוב משני העולמות.

הבחירה ההיברידית משקפת איך הבעיה הזאת תיפתר באמת בקנה המידה של זאפ. אי אפשר
לשלוח מיליוני מוצרים ל-`LLM`, והתאמת דמיון טהורה מפספסת ניואנסים סמנטיים.
`pipeline` בשני שלבים נותן מהירות איפה שצריך אותה (סינון ראשוני) ואינטליגנציה
איפה שצריך אותה (החלטות סופיות).

ה-`stack`: שפת `Python`, ספריית `sentence-transformers` עם המודל
`paraphrase-multilingual-mpnet-base-v2` (חינם, מקומי, רב-לשוני), ומודל
`OpenAI gpt-4o-mini` (זול, פלט `JSON` יציב, מופשט מאחורי שתי פונקציות עזר
כך שהחלפת ספק היא שינוי בקובץ אחד).

## 3. הדאטהסט

הדאטהסט סינתטי, לא מגורד - החלטה מודעת.<br> הניסוח של המשימה אומר "קיבלת רשימת
מוצרים", גירוד של אתר ייצור של מעסיק פוטנציאלי בלי רשות זה לא נכון, ודאטהסט
מבוקר מאפשר להכניס לתוך קובץ קטן אחד כל מקרה מסובך (תעתיק עברי, שמות שמכילים
רק מספרי דגם, התאמות-קרובות שאסור לאחד) שאפשר לעבור עליו בעיניים.

יש בו **131 רישומים, 33 מוצרים קנוניים, 6 קטגוריות** (טלפונים, טלוויזיות,
אוזניות, לפטופים, מכונות כביסה, טאבלטים). <br>כולל גם מקרי **"חייב לאחד"**
(`Samsung Galaxy S24 Ultra 256GB` מול `סמסונג גלקסי S24 אולטרה 256GB`) וגם מקרי
**"אסור לאחד"** (`"256GB` vs `512GB`, `XM5` vs `XM4`, `Pro` vs `Pro Max`,
`55"` vs `65`) כך שהמערכת נבדקת גם על `precision` וגם על `recall`.

הסקיילביליות היא **ארכיטקטונית**, לא בכוח גס: שלב ה-`embeddings` הוא `O(n²)`
בזוגות (ניתן להחלפה ב-`FAISS`/`ANN` בקנה מידה גדול), ושלב ה-`LLM` נוגע רק
בקלאסטרים מעורפלים - כך שהעלות גדלה עם ה*עמימות*, לא עם סך הרישומים.

## 4. ארכיטקטורת ה-`pipeline`

```
products.csv (131 listings)
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ STAGE 1 - EMBEDDINGS  (src/embeddings.py)                 │
│ Pass 1: pairwise within-category at sim ≥ 0.87 → clusters │
│ Pass 2: assign leftover singletons to centroids ≥ 0.65    │
│ Output: 24 candidate clusters + 9 leftover singletons     │
│ Goal:   HIGH RECALL - catch everything                    │
└───────────────────────────────────────────────────────────┘
        │  (only the ~33 items Stage 1 was unsure about)
        ▼
┌───────────────────────────────────────────────────────────┐
│ STAGE 2 - LLM REFINEMENT v2  (src/llm_refine_v2.py)       │
│ Pass 1 - SPLIT (gated by variant-token agreement)         │
│ Pass 2 - MATCH (gated by centroid similarity)             │
│ Pass 3 - MERGE                                            │
│              (gated by centroid distance + sibling guard) │
│ Output: ~29–30 final clusters, 0 unmatched singletons     │
│ Goal:   HIGH PRECISION - only merge real duplicates       │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│ STAGE 3 - PRICE CONSOLIDATION  (src/deduplicator.py)      │
│ canonical_name = longest in cluster                       │
│ min_price, max_price, savings_pct, listing_ids            │
└───────────────────────────────────────────────────────────┘
        │
        ▼
deduplicated_products.csv
+ deduplicated_products.audit.json
```

## 5. שלב 1 - `Embeddings`

שתי החלטות עיצוב חשובות כאן.

**סינון לפי קטגוריה.** בלעדיו, טלפון עם `256GB` משתרשר ללפטופ עם `256GB`
דרך טאבלט, ויוצר קלאסטר אחד ענק חוצה-קטגוריות. השיפור הגדול ביותר.

**שני מעברים עם ספים שונים.** המעבר הראשון מבצע השוואה בזוגות בתוך כל קטגוריה
ב-cosine `≥ 0.87`, ומשתמש ב-`union-find` כדי לבנות קלאסטרים מגרף הזוגות שנוצר.
זה מכוון ל-**`recall` גבוה** - עדיף לאחד יותר מדי ולתת לשלב 2 לפצל, מאשר
לפספס כפילות אמיתית לתמיד.<br> המעבר השני מקצה את הסינגלטונים שנותרו ל*סנטרואידים*
של קלאסטרים קיימים בסף הרבה יותר רך של `≥ 0.65`. סנטרואידים הם הממוצע של
ה-`embeddings` של קלאסטר, אז הם יציבים יותר מכל שם בודד - ובטוח להשתמש בסף
נמוך יותר בלי בעיות השרשור של המעבר הראשון.

הסף 0.87 נבחר על ידי תיוג של כל זוג בתוך-קטגוריה מול האמת היסודית הסינתטית,
ובדיקה איפה כפילויות אמיתיות מול לא-כפילויות נופלות על סקלת הדמיון. שתי
ההתפלגויות חופפות מאוד בטווח 0.65–0.90 - **אין פער נקי**, אז שום סף יחיד לא
יכול להיות מושלם. <br>הסף 0.87 מציב את רוב הלא-כפילויות משמאל לקו תוך שהוא משאיר
מספיק כפילויות מימין; כל השאר מוצלים על ידי המעבר השני וה-`LLM` (גרפים מלאים
והיסטוריית האיטרציות נמצאים במסמך הארוך).

התוצאה: 24 קלאסטרים נקיים שמכסים 122 מתוך 131 רישומים, ועוד 9 סינגלטונים
שנשארו. ה-9 הם בעיקר תעתיקים עבריים (`אירפודז פרו 2`) ושמות שמכילים רק מספרי
דגם (`Samsung WW80T534 8kg`) - דברים בלי שום חפיפה לקסיקלית עם המקבילות שלהם
שאף מודל `embedding` לא יכול לפתור.

## 6. שלב 2 - שיפור `LLM`

שלושה מעברים, כל אחד מטפל במצב כשלון אחר של שלב 1.

**מעבר 1 - פיצול.** עבור כל קלאסטר מועמד, שואלים את `GPT-4o-mini` _"אילו
מאלה הם באמת אותו מוצר?"_ ומאפשרים לו להחזיר קבוצות. ה-`prompt` נוקשה: אותו
מותג/דגם/אחסון/גודל/דור, ושמות בעברית ובאנגלית של אותו מוצר **כן** נחשבים
אותו מוצר. בדיקת שפיות שומרת על הקלאסטר המקורי כפי שהוא אם ה-`LLM` משמיט או
ממציא מזהים. זה מה שמפצל את קלאסטר ה-`iPhone` הענק בן 16 הפריטים ל-`iPhone 15`,
`iPhone 16`, `iPhone 16 Pro` ו-`iPhone 16 Pro Max`.

**מעבר 2 - התאמת סינגלטונים.** מאחדים את 9 הסינגלטונים המקוריים יחד עם כל
הקבוצות החדשות בנות פריט אחד שהמעבר הראשון יצר (באג מוקדם: התעלמות מיתומי
המעבר הראשון השאירה כפילויות תקינות תקועות). עבור כל סינגלטון מגבילים את
המועמדים לקלאסטרים מאותה קטגוריה ושואלים את ה-`LLM` אם הוא שייך לאחד מהם.
כאן נפתרים התעתיקים העבריים.

**מעבר 3 - איחוד שברי קלאסטרים.** אחרי המעבר השני, אותו מוצר עדיין יכול לשבת
בשני קלאסטרים נפרדים (לדוגמה: קלאסטר `Sony XM5` עברי וקלאסטר `Sony XM5`
אנגלי). המעבר השלישי בוחר מייצג מכל קלאסטר מרובה-פריטים, מתייחס אליו
כ"סינגלטון" מול כל הקלאסטרים האחרים מאותה קטגוריה, ומשתמש ב-`union-find`
לאיחודים טרנזיטיביים במכה אחת.

### השוואת v1 מול v2: שיפור עם שערי ביטחון

הגרסה הראשונה (`src/llm_refine.py`) קוראת ל-`LLM` על כל מועמד. ברירת המחדל
הנוכחית (`src/llm_refine_v2.py`) קוראת רק כשיש אי-ודאות אמיתית:

- **שער 1** (פיצול במעבר הראשון): מדלגים על ה-`LLM` אם לכל פריט בקלאסטר יש
  אותם טוקני וריאנט (`256gb`, `pro max`, `xm5`, `s24`, `אולטרה` וכו'). אין
  אי-הסכמה על וריאנטים = אין מה לפצל.
- **שער 2** (התאמה במעבר השני): אם הדמיון של הסינגלטון לסנטרואיד של איזשהו
  קלאסטר הוא `≥ 0.85`, מאחדים אוטומטית בלי קריאת `LLM`.
- **שער 3** (איחוד במעבר השלישי): שואלים את ה-`LLM` רק אם הסנטרואידים של
  הקלאסטרים נמצאים במרחק של `≥ 0.65` **וגם** שני הקלאסטרים לא הגיעו מאותו
  הורה במעבר הראשון. התנאי השני הוא **שמירת האחים** - בלעדיה, גרסה מוקדמת של
  v2 איחדה בשמחה את `iPhone 15` חזרה לתוך `iPhone 16`, וביטלה את העבודה
  שהמעבר הראשון בדיוק עשה.

העיקרון מאחורי השערים: **כל קריאת `LLM` היא גם סיכון.** ב-`temperature=0`
המודל עדיין משתנה בין ריצות. פחות קריאות פירושו פחות הזדמנויות לטעות.

## 7. שלב 3 - איחוד מחירים

ברגע שיודעים אילו רישומים הם כפילויות, השלב הזה הוא במכוון בערך 30 שורות.
לכל קלאסטר: בוחרים שם קנוני, מחשבים `min_price`, `max_price`, `savings_pct`,
ושומרים את ה-`listing_ids` המקוריים לעקיבות. סינגלטונים נשמרים כפי שהם -
מוצר עם מוכר אחד הוא עדיין מוצר.

לצד ה-`CSV` העסקי הסופי, ה-`pipeline` כותב עכשיו גם
`deduplicated_products.audit.json` - קובץ explainability קטן שמתעד החלטות
מייצגות משלב 2 עם שדות כמו `used_ai`, `confidence` ו-`reason`. הוא לא מחליף את
הקטלוג המאוחד; הוא רק עוזר לקורא להבין למה התאמות מסוימות קרו.

השם הקנוני הוא **המחרוזת הארוכה ביותר בקלאסטר**. זה `proxy` חינמי: שמות ארוכים
יותר כמעט תמיד נושאים יותר טוקנים מזהים (`Samsung Galaxy S24 Ultra 256GB
Titanium Black` ארוך יותר מ-`Galaxy S24` כי הוא כולל את המותג, הוריאנט, האחסון
והצבע). אורך הוא לא ממש אינפורמטיביות, אבל בכותרות מוצרים אמיתיות הוא מתאם
מספיק חזק כך שה-`proxy` הזול נותן ~95% ממה שגישת `LLM` הייתה נותנת תמורת 0%
מהעלות. אם כותרות מוכרים אמיתיות אי פעם ייראו כמו `!!! SALE !!! ... BEST PRICE !!!`,
זה הזמן להחליף את זה.

## 8. תוצאות

הטבלה הבאה מתארת ריצה מייצגת על הדאטהסט בן 131 השורות (מטריקות מלאות
ב-[`data/compare_approaches.py`](data/compare_approaches.py)). בגלל
אי-דטרמיניזם קל ב-`LLM`, מספר הקלאסטרים ותוצאת ה-`F1` יכולים לזוז מעט בין
ריצות, אבל v2 נשאר סביב ~77% ויציב יותר.

| מטריקה                | v1 (תמיד-`LLM`) | v2 (שערים, ברירת מחדל) |
| --------------------- | --------------: | ---------------------: |
| קריאות `API`          |              73 |                 **57** |
| קלאסטרים מרובי-פריטים |              28 |                     29 |
| סינגלטונים לא-משובצים |               0 |                      0 |
| `Precision`           |          60.73% |             **64.57%** |
| `Recall`              |         100.00% |                 97.01% |
| **`F1`**              |          75.56% |             **77.53%** |

**הגרסה v2 מנצחת:** `F1` גבוה יותר (מדד הסיכום הטוב ביותר), 22% זול יותר, יותר
מדויק, ויציב יותר בין ריצות (~77% קבוע מול תנודות של 69–77% ב-v1). v1 תופס
6 זוגות כפילויות אמיתיות נוספים (100% מול 97% `recall`), אבל במחיר של יותר
איחודים שגויים ויותר קריאות `API`. שתי הגרסאות משבצות כל מוצר בקלאסטר על
הדאטהסט הזה.

זמן ריצה מקצה לקצה: הרבה מתחת לדקה. עלות לריצה: פחות מסנט אחד.

## 9. הסתייגויות ומה הייתי עושה עם יותר זמן

- **הספים נבחרו לפי אינטואיציה ועוד סבב אחד של תיקון באגים**, לא לפי
  `grid search`. עם שונות של ±8 נקודות `F1` בין ריצות בדאטהסט בן 131 שורות,
  סריקה הייתה מתאימה את עצמה יתר על המידה ל-`CSV` הסינתטי. הזמן הנכון לכיוונון
  הוא על דאטה אמיתי של זאפ עם החלטות השערים מתועדות.
- **שם קנוני = המחרוזת הארוכה ביותר** הוא `proxy` חינמי. אפשר להחליף אותו
  בבוחר מבוסס-`LLM` אם כותרות אמיתיות יתבררו כרועשות.
- **שלב 3 הוא במכוון מינימלי.** כל הערך הוא בלהחליט _אילו_ רישומים הם
  כפילויות; ברגע שזה נגמר, ה-`group-by` הוא טריוויאלי.
- **אין מטא-דאטה של מוכרים.** פריסה אמיתית הייתה נושאת `seller_id` לכל
  רישום כדי שהשורה המאוחדת תוכל להציף "הזול ביותר אצל: מוכר X".

## 10. העיקרון

העיקרון שחוזר בכל הבחירות הוא פשוט: **להשתמש בכל כלי רק כשהוא הכלי הנכון
לעבודה.** לכן `embeddings` באים לפני `LLM`, לכן שלב 1 מכוון ל-`recall` גבוה,
לכן שלב 2 מוסיף שערים, ולכן שלב 3 בוחר בשם הארוך ביותר. `Embeddings` הם זולים
וקירוביים; `LLMs` יקרים וחכמים; `group-by` הוא מכני ודטרמיניסטי. ה-`pipeline`
מרכיב אותם כך שכל אחד עושה בדיוק את מה שהוא טוב בו, ושום דבר מעבר.
